# Kanawa Soft ERP 15.2 — environment template
# Never commit the real .env file.
PORT=3000
JWT_SECRET=replace-with-a-long-random-secret
NODE_ENV=production
# Optional: comma-separated allowed web origins
CORS_ORIGINS=https://kanawasoft.shop,https://kanawasoftv15.netlify.app
